GLOBAL TODOs

TODO: Do solid pass of all property and method attributes
TODO: Do solid pass of all getter and setter methods (as well as making properties Dependent
      or "psudeo"-Dependent (see estimatedParamJacobianMap)

TODO: Abstract wherever you can (ProjectileState < State? BatchEstimator < Estimator?)

TODO: Add tempStorage to deriv functions. That way not recomputing a ton of variables on each
      force model or Jacobian call



ESTIMATOR TODOs

TODO: propagator.getProjectileAt(time) to update projectile dynState, then pass that
to various funcs? (e.g., sensors for taking samples) Rather than passing dynState
directly. Also update projectile dynState in propagation loop? Philosophy is should
pass projectile and earth, not abstract state vector
TODO: Handle cross-covariance output in state and params (e.g., CX0 and CX2)
TODO: projectile.updateState(time, state)
TODO: projectile.updateParams() for aero coeffs and derivs during propagation
